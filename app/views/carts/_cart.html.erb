<% if current_order.order_items.size >= 1 && current_order.subtotal > 0%>
<%= link_to "#{current_order.order_items.size} items in cart with a value of Rs.#{current_order.subtotal}", carts_path, style: 'font-size:20px; color:white; background-color:black; ' %>
<% end %>